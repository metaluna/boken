<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @story.title %>
</p>

<p>
  <strong>Abstract:</strong>
  <%= @story.abstract %>
</p>

<%= link_to 'Delete', @story, method: :delete, data: { confirm: "Do you really want to delete the story '#{@story.title}'?" } %> |
<%= link_to 'Edit', edit_story_path(@story) %> |
<%= link_to 'Back', stories_path %>
